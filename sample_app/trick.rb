#attr_accessbile :name, :email

#rails c --sanbox

#tail -f log/development.log

#add_index :users, :email, :unique => true
#remove_index :users ,:email

#rake db:test:prepare

#validates :name, :presence => true
#                 :length => { :maximum => 50 }
#                 :format => { :with => regex }

#rspec
#  before(:each) do
  
#  end 
